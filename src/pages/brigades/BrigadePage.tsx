import { PageHeader } from "@/components/PageHeader";

export default function BrigadePage({ multi = false }: { multi?: boolean }) {
    return (
        <>
            <PageHeader title="Brygada" />
            <div className="page">TODO</div>
        </>
    );
}
