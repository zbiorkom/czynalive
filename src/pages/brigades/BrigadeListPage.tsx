import { PageHeader } from "@/components/PageHeader";

export default function BrigadeListPage({ multi = false }: { multi?: boolean }) {
    return (
        <>
            <PageHeader title="Rozkład brygad" />
            <div className="page">TODO</div>
        </>
    );
}
